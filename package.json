{
  "name": "briec2000",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "bunx astro dev",
    "build": "bun run update-date && node node_modules/.bin/astro build",
    "preview": "bunx astro preview",
    "astro": "bunx astro",
"update-date": "bun -e \"const fs = require('fs'); const date = new Date().toLocaleDateString('fr-FR', {day: 'numeric', month: 'long', year: 'numeric'}); fs.writeFileSync('src/data/build-info.json', JSON.stringify({lastUpdate: date, buildDate: new Date().toISOString()}, null, 2)); console.log('Date mise à jour:', date);\""
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.10",
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.1.5",
    "astro": "^6.1.3",
    "tailwindcss": "^4.1.5"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}